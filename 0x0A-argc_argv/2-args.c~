#include "main.h"
#include <stdio.h"

/**
 *main - Entry point
 *@argc: number os arguements passed
 *@argv: A pointer to a string
 *Return: Always 0
 */

int main(int argc, char **argv)
{
	int i;

	for (i = 0; i < argc; i++)
	{
		printf("%s", *(argv + i));
		printf("\n");
	}
	return (0);
}
