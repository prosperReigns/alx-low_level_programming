#include <stdio.h>

/**
*main - Number 0 - 9
*Description: Prints single digit in base10
*Return: Always 0 (successful)
*/

int main(void)
{
int num = 0;
while (num < 10)
{
printf("%d", num);
num++;
}
return (0);
}
