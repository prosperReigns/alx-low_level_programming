#include "main.h"
#include <stdlib.h>

/**
 *free_grid - release allocated memory
 *@grid: pointer to 2d array
 *@height: size of grid
 *Return: Always void
 */

void free_grid(int **grid, int height)
{
	int i;

	for (i = 0; i < height; i++)
		free(grid[i]);
}
