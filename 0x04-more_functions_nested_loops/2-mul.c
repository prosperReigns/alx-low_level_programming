#include "main.h"

/**
 *mul - multiply two numbers
 *@a: first operand
 *@b: second operand
 *Returns: Always multiply
 */

int mul(int a, int b)
{
	int multiply;

	multiply = a * b;
	return (multiply);
}
