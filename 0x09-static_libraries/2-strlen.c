#include "main.h"
#include <string.h>

/**
 *_strlen - determines the lenght of a string
 *@s: an array of characters
 *Return: Always lenght
 */

int _strlen(char *s)
{
	int lenght;

	lenght = strlen(s);
	return (lenght);
}
