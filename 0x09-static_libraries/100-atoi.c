#include "main.h"
#include <stdlib.h>

/**
 *_atoi - converts a stribg into an integer
 *@s: an array of characters
 *Return: Always res
 */

int _atoi(char *s)
{
	int res;

	res = atoi(s);
	return (res);
}
