#include "main.h"
#include <ctype.h>

/**
 *_isupper - check if a character is uppercased
 *@c: a character as an arguement
 *Return: always res
 */

int _isupper(int c)
{
	int res;

	res = isupper(c);
	return (res);
}
