#include "main.h"
#include <stdlib.h>
#include <string.h>
/**
 *
 *
 *128 64 32 16 8 4 2 1
 *
 */

void print_binary(unsigned long int n)
{
	unsigned i;
    for (i = n << 1; i > 0; i = i / 2)
    {
	    if ((n & i) == 0)
		    _putchar('0');
	    if ((n & 1) == 1)
		    _putchar('1');
    }
}
