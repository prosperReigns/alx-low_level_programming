#include "main.h"
#include <string.h>

/**
 *_srtlen - The lenght of a string
 *@s: points to the address of a data value
 *Return: Always len
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
